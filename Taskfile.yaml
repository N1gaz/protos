version: "3"

tasks:
  generate:
    aliases:
      - gen
    descs: Generate protobuf code
    cmds:
      - protoc -I proto/sso --go_out=gen/go/sso --go_opt=paths=source_relative --go-grpc_out=gen/go/sso --go-grpc_opt=paths=source_relative proto/sso/sso.proto